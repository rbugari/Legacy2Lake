"""Legacy compatibility package for historical imports.

This package preserves old `services.*` import paths used by legacy tests and
older scripts, while delegating implementation to the current
`apps.api.services.*` modules.
"""
