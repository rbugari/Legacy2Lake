[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short -m "not integration"
markers =
    integration: tests that require integration-level dependencies
filterwarnings =
    ignore::DeprecationWarning
asyncio_mode = auto
