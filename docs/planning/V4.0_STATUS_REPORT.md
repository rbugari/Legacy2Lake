# v4.0 Release — Estado Consolidado
**Última actualización:** 2026-03-03  
**Estado general:** 🟢 100% Completo — En producción, fase de estabilización post-launch  
**Launch:** Marzo 2026 ✅ LANZADO

---

## ✅ COMPLETADO

### Feature 1: Zero-Hardcode Generation ✅
- Prompts dinámicos desde base de datos (`utm_prompts`)
- Versionado automático por trigger (safety net, READ-ONLY para ADMIN)
- Prompts globales para toda la plataforma (sin customización por tenant en v4.0)
- Historial read-only para ADMIN (análisis y seguridad)
- NO hay UI de rollback — trigger automático es el único mecanismo

### Feature 2: Deep Forensic Triage ✅ (100%)
- Análisis estadístico por columna/campo
- Detección automática de PII
- Scoring de calidad por campo
- Recomendaciones automáticas
- **Schema Viewer PK/FK Detection** (Feb 18, 2026):
  - Detección two-pass en LibrarianService
  - Fixed silent parser failure (ForeignKeyColumnConstraint bug)
  - SQL lineage integration para tracking de columnas
  - Visual indicators (emerald dots, opacity, badges PK/FK)
  - 95% schema detection rate (vs 40% pre-fix)
  - 90% PK/FK detection (nueva capacidad)

### Feature 3: Real-Time Validation ✅
- Validación sintáctica durante generación
- Validación semántica (referencias a columnas)
- Auto-corrección en loop
- Reducción de reintentos LLM

### Feature 4: UI Componentization ✅
- Performance Crisis Resolution (Sprint 14): 95%+ reducción (30+ req/s → 0.1 req/s)
- Unified Sidebar Architecture con lifted-state pattern
- Execution Status Indicators (⚠️ no data, 🔄 processing, ✅ ready)
- Circular Dependencies eliminadas (zero infinite re-render loops)
- SchemaViewer Component con asset selector, table explorer, PK/FK badges
- TypeMismatchViewer Component para diff columnar source vs destination
- Triage Navigation Sync (Graph + Grid + Schema tabs)
- StatusDots en sidebar secciones (verde / amarillo por disponibilidad de datos)

### Feature 5: Role-Based Access Control ✅ (v3.9 → completado en v4.0)
- 4 roles: ADMIN, MANAGER, COLLABORATOR, VIEWER
- `list_projects` filtra por rol:
  - ADMIN → todos los proyectos, todos los tenants
  - MANAGER → todos los proyectos de su tenant
  - COLLABORATOR/VIEWER → solo proyectos con membresía explícita
- `SupabasePersistence` bypasses tenant filter si `role == ADMIN`

### Feature 6: Platform Admin & Ghost Mode ✅
- Dashboard cross-tenant para ADMIN
- All Users tab con filtros (tenant, rol, búsqueda)
- Reset de contraseña para cualquier usuario
- Ghost Mode (impersonación) funcional
- Tenant Management (crear, editar, eliminar)

---

## 🔧 BUGS CORREGIDOS (Esta sesión — Feb 25-27, 2026)

### Bug #1: `utm_tenants.client_id` no existe ✅ FIXED
**Síntoma:** Error 500 en todas las requests cuando Ghost Mode estaba activo  
**Causa:** `get_identity()` en `dependencies.py` consultaba la columna `client_id` en `utm_tenants`, columna eliminada en migración 024 (v3.9).  
**Fix:** `apps/api/routers/dependencies.py` — en el bloque de impersonación, `target_client_id` ahora se toma directamente del header `X-Client-ID` del admin que inicia la sesión, sin consulta adicional a `utm_tenants`.

```python
# ANTES (ROTO)
tenant_res = db.client.table("utm_tenants").select("client_id")\
    .eq("tenant_id", target_user["tenant_id"]).execute()
target_client_id = tenant_res.data[0]["client_id"] if tenant_res.data else "UNKNOWN"

# DESPUÉS (CORRECTO)
target_client_id = x_client_id  # Usa el del header enviado por el admin
```

### Bug #2: Admin page retorna 403 en Ghost Mode ✅ FIXED
**Síntoma:** `/auth/admin/users` y `/auth/tenants` respondían 403 cuando el admin tenía activo el Ghost Mode en el browser.  
**Causa:** `fetchWithAuth` en `auth-client.ts` siempre enviaba `X-Impersonate-User-ID` si estaba en localStorage, incluso desde la página `/admin`. El backend interpretaba al admin como el usuario impersonado, que no tiene role ADMIN.  
**Fix (2 partes):**

1. `apps/web/app/lib/auth-client.ts` — nuevo parámetro `skipImpersonation?: boolean` en `FetchAuthOptions`
2. `apps/web/app/admin/page.tsx` — wrapper local de `fetchWithAuth` que fuerza `skipImpersonation: true` en todas las calls de la página de administración

```typescript
// auth-client.ts
export interface FetchAuthOptions extends RequestInit {
    skipImpersonation?: boolean;
}

// admin/page.tsx (fuera del componente)
const fetchWithAuth = (endpoint: string, options: any = {}) => 
    baseFetchWithAuth(endpoint, { ...options, skipImpersonation: true });
```

### Bug #3: StrategicIntelligenceHub — JSX parsing error ✅ FIXED
**Síntoma:** Error de compilación Turbopack en `StrategicIntelligenceHub.tsx` — template literal JSX mal escapado.  
**Fix:** Corrección del escape en el bloque `<style jsx>`.

### Bug #4: UserManagement — desbordamiento de botones en modal ✅ FIXED
**Síntoma:** Botones del modal "Create/Edit User" salían del área visible.  
**Fix:** Ajuste de layout y padding en `UserManagement.tsx`.

### Bug #5: Dropdown legibility en StrategicIntelligenceHub ✅ FIXED
**Síntoma:** Texto gris sobre fondo gris en los `<select>` del hub.  
**Fix:** `bg-slate-800 text-white` sobre las opciones del dropdown.

---

## 🔧 E2E STABILIZATION (2026-03-03)

### Bugs corregidos en pipeline end-to-end:

| ID | Descripción | Archivo Modificado | Fix |
|---|---|---|---|
| BUG-E01 | Drafting BLOCKED: `run_full_migration` rechazaba el pipeline porque el status llegaba como `TRIAGED` | `apps/api/routers/transpile.py` | `update_project_status(project_id, "DRAFTING")` sincrónico antes de lanzar background task |
| BUG-E02 | Paneles de logs mostraban ejecución anterior al entrar a una fase (logs stale) | `apps/web/app/components/stages/DraftingView.tsx`, `RefinementView.tsx` | Ambos componentes consultan `discovery/status` en mount y solo cargan logs según el status actual |
| BUG-E03 | Governance (Stage 4) no tenía Quick Info en el sidebar — único stage sin él | `apps/web/app/config/sidebar-sections.ts` | Agregado bloque `quick-info` al top del array de `SIDEBAR_SECTIONS[4]` |

### Lógica de carga de logs (BUG-E02):

| Status al montar | Acción |
|---|---|
| `TRIAGED` / transición hacia fase | Logs vacíos — pantalla limpia |
| `DRAFTING` / `REFINING` (run activo) | Carga logs + arranca polling |
| `DRAFTED` / `REFINED` / beyond (completado) | Carga logs históricos + marca fase completa |

---

## 🔧 POST-LAUNCH STABILIZATION (2026-03-02)

### Bugs corregidos en producción:

| ID | Descripción | Fix |
|---|---|---|
| BUG-001 | `DELETE /catalog` → 404 (model_id como URL en path) | `model_id` en body |
| BUG-002 | Admin reset-password → 404 | URL endpoint corregida |
| BUG-003 | Model Test Connection ⚡ 502 en configs malas | Feature + diagnóstico correcto |
| BUG-004 | Vault sin borrado de providers | Feature `POST /vault/delete` |
| BUG-005 | Agent Matrix "Apply to All" no persistía (stale state React) | `saveMatrix(newMatrix)` directo |
| BUG-006 | `quick_assessment_service` KeyError `deployment_name`/`model` | Keys alineadas con `resolve_agent_model` |
| BUG-007 | Solution Settings en blanco (race condition) | `Promise.all` paralelo + normalize dinámico |
| BUG-008 | TechnologyMixer no mostraba tech guardada (nesting incorrecto) | `settingsData.settings?.target_tech` |
| BUG-009 | Agentes usando glm-5 en lugar de qwen | DB + UX del batch fix |

### Tablas activas (adicionadas en v4.0 post-launch)
| Tabla | Propósito |
|---|---|
| `utm_model_catalog` | Catálogo de modelos LLM por tenant |
| `utm_provider_vault` | API keys y endpoints por provider/tenant |
| `utm_agent_matrix` | Asignación modelo→agente por tenant |

---

## ⏳ PENDIENTE (Backlog v4.1)

### P1 — Design system audit & Visual polish
- Revisar consistencia de tokens CSS en todos los componentes
- Accessibility: WCAG 2.1 AA compliance (parcialmente cubierto)

### P2 — Final integration testing
- Ciclo completo: Discovery → Triage → Drafting → Refinement → Governance → Handover
- Validar con usuario impersonado (demo1) que el flujo funciona end-to-end

### P3 — Database indices
> Los índices están identificados pero el usuario los gestiona manualmente.  
> Ver: `utm_projects(tenant_id)`, `utm_execution_logs(project_id, phase)`, `utm_objects(project_id)`

### P4 — Backend query optimization
- Queries Supabase toman 1-4s cada una
- Future: batch compound queries, Redis cache, WebSockets para real-time

### P5 — Tenant data isolation test
- Verificar que ningún tenant puede ver datos de otro en ningún endpoint
- Especialmente endpoints del sistema que no filtran por tenant

---

## 🗄️ Estado de la Base de Datos

### Tablas Activas (v4.0-relevant)
| Tabla | Propósito | Estado |
|---|---|---|
| `utm_tenants` | Organizaciones (sin `client_id` desde migración 024) | ✅ Activa |
| `utm_users` | Usuarios individuales con role | ✅ Activa |
| `utm_projects` | Proyectos por tenant | ✅ Activa |
| `utm_project_members` | Acceso granular a proyectos | ✅ Activa |
| `utm_prompts` | Prompts globales (Zero-Hardcode) | ✅ Activa |
| `utm_prompts_history` | Historial auto (trigger) | ✅ Activa |
| `utm_objects` | Assets del proyecto | ✅ Activa |
| `utm_execution_logs` | Logs por phase | ✅ Activa |
| `utm_process_locks` | Bloqueos de proceso | ✅ Activa |
| `utm_system_catalog` | Catálogo de orígenes/destinos/cartridges | ✅ Activa |
| `utm_agent_catalog` | Agentes dinámicos | ✅ Activa |

### Migraciones Ejecutadas
- **024:** Eliminación de `client_id` en `utm_tenants`  
- **025:** Eliminación de `org_name` en `utm_tenants`

> ⚠️ **NOTA IMPORTANTE:** El backend tenía código que consultaba `utm_tenants.client_id` (bug #1). Está corregido en código. No requiere cambio de DB.

---

## 🏗️ Arquitectura del Rol ADMIN

### Headers de autenticación
| Header | Propósito |
|---|---|
| `X-Tenant-ID` | UUID del tenant del usuario (requerido, validado como UUID v4) |
| `X-User-ID` | UUID del usuario autenticado |
| `X-Client-ID` | UUID del cliente (legacy — mismo valor que tenant_id para tenants nuevos) |
| `X-Role` | Rol del usuario (ADMIN / MANAGER / COLLABORATOR / VIEWER) |
| `X-Impersonate-User-ID` | [Solo Ghost Mode] UUID del usuario a impersonar |

### Ghost Mode flow
```
Admin activa Ghost Mode → localStorage.setItem("x_impersonate_user_id", targetUserId)
↓
fetchWithAuth envía X-Impersonate-User-ID (salvo skipImpersonation=true)
↓
get_identity() detecta admin + impersonate → devuelve identidad del usuario objetivo
↓
SupabasePersistence usa tenant_id del usuario objetivo
↓
list_projects filtra por ese tenant_id
```

### Página de Admin (skipImpersonation)
La página `/admin` usa un wrapper local que fuerza `skipImpersonation: true` — nunca propaga el Ghost Mode hacia sus propias APIs de gestión. Esto garantiza que el admin puede gestionar la plataforma sin importar si hay una sesión fantasma activa.

---

## 📁 Archivos Modificados (Esta Sesión)

| Archivo | Cambio |
|---|---|
| `apps/api/routers/dependencies.py` | Eliminada consulta a `utm_tenants.client_id` en bloque de impersonación |
| `apps/web/app/lib/auth-client.ts` | Nuevo parámetro `skipImpersonation`, interfaz `FetchAuthOptions` |
| `apps/web/app/admin/page.tsx` | Wrapper local de `fetchWithAuth` con `skipImpersonation: true` |
| `apps/web/app/components/settings/StrategicIntelligenceHub.tsx` | Fix JSX parse error + dropdown legibility |
| `apps/web/app/components/settings/UserManagement.tsx` | Fix modal button overflow |

---

## 📊 Métricas de Calidad v4.0

| Métrica | Target | Actual | Estado |
|---|---|---|---|
| Schema Detection | 80% | 95% | 🎯 Superado |
| PK/FK Detection | 75% | 90% | 🎯 Superado |
| Performance Improvement | 50% | 95% | 🎯 Superado |
| Code Validation Pass Rate | 90% | 92% | ✅ Alcanzado |
| Backend Error 500 en Ghost Mode | 0 | 0 (fixed) | ✅ Alcanzado |
| Admin Page 403 en Ghost Mode | 0 | 0 (fixed) | ✅ Alcanzado |
| Features Complete | 100% | 98% | 🟡 Casi completo |

---

## 🚀 Para el Deploy Final a Producción

### Variables de entorno
- ❌ No hay nuevas variables desde v4.0 GA (LLM keys van en DB, no en .env)
- ✅ Variables de .env necesarias: `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `R2_*`, `JWT_SECRET`

### Migraciones de DB
- ✅ No hay migraciones nuevas en los 3 bugs del 03/03/2026 (todos fueron fixes de código)
- ✅ Migraciones ejecutadas hasta 025 (ya en prod)

### Rebuild necesario
- **Backend**: Reiniciar después de cambios en `transpile.py`
- **Frontend**: Rebuild después de cambios en `DraftingView.tsx`, `RefinementView.tsx`, `sidebar-sections.ts`

### Test manual pre-deploy
1. Login → crear proyecto → subir fuentes → Discovery completo
2. Triage → verificar que logs arrancan limpios
3. Drafting → click "Run Pipeline" → verificar que arranca sin BLOCKED
4. Refinement → verificar que sidebar tiene Quick Info en Governance
5. Login como admin → activar Ghost Mode sobre demo1 → verificar lista de proyectos
6. Con Ghost Mode activo → navegar a `/admin` → verificar que carga sin 403

---

*v4.0 Final Scope · Legacy2Lake*
