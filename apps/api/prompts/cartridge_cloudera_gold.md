# Cloudera Spark Gold Layer Cartridge Rules

## 🎯 CRITICAL COMPLIANCE REQUIREMENTS (Zero-Hardcode Policy)

### 1. MANDATORY L2L MODERNIZATION TRACE HEADER
```python
# ================================================================
# L2L MODERNIZATION TRACE: {{layer}} Layer - {{asset_name}}
# ================================================================
# Source Asset: {{source_asset_name}}
# Source Technology: {{source_tech}}
# Target Platform: Cloudera CDP Datahub (Spark)
# Medallion Layer: gold
# Component Type: Data Processing / Aggregation
# Business Entity: {{business_entity}}
# Load Strategy: {{load_strategy}}
# Generated At: {{timestamp}}
# Generated By: L2L Agent-C (Cartridge: ClouderaSpark v1.0 - Gold)
# ================================================================
```

### 2. ZERO-HARDCODE CONFIGURATION
**❌ FORBIDDEN: Hardcoded values anywhere in code**
- NO hardcoded paths: `hdfs://...`, `/ozone/...`
- NO hardcoded table names or schemas.

**✅ REQUIRED: All paths and configs from orchestrator context**
```python
def execute_task(spark, config):
    # Extract ALL values from config dictionary
    catalog = config.get('catalog_name', 'hive_metastore')
    silver_schema = config.get('silver_schema', 'silver_curated')
    gold_schema = config.get('gold_schema', 'gold_analytics')
```

### 3. BUSINESS LOGIC & AGGREGATION
- Implement explicit windowing or `groupBy` semantics using standard PySpark DataFrame mechanisms.
- Read active records (`is_current = true`) from Silver.
- Persist curated facts and dimensions into partitioned Gold tables matching Cloudera standards (Iceberg or Parquet/Hive).

### 4. FUNCTION STRUCTURE
**✅ ONLY expose `execute_task(spark, config)` - NO main() function**

## 🎯 OUTPUT REQUIREMENTS
1. Return ONLY valid Cloudera PySpark code.
2. Emphasize aggregation.
3. Clean logging and robust error handling.
4. L2L Header for the `gold` layer.
