# Cloudera Spark Direct Transcription Cartridge Rules

## 🎯 CRITICAL COMPLIANCE REQUIREMENTS (Zero-Hardcode Policy)

### 1. MANDATORY L2L MODERNIZATION TRACE HEADER
```python
# ================================================================
# L2L MODERNIZATION TRACE: Direct Translation - {{asset_name}}
# ================================================================
# Source Asset: {{source_asset_name}}
# Source Technology: {{source_tech}}
# Target Platform: Cloudera CDP Datahub (Spark)
# Medallion Layer: N/A (Direct Transcription)
# Component Type: Data Script Translation
# Business Entity: {{business_entity}}
# Generated At: {{timestamp}}
# Generated By: L2L Agent-C (Cartridge: ClouderaSpark v1.0 - Direct)
# ================================================================
```

### 2. CLOUDERA SPECIFIC OPTIMIZATIONS
- **Format:** Default to Iceberg when creating or managing tables unless otherwise specified in configuration.
- **Spark Conf:** Recognize that this code will run on Cloudera Datahub clusters, taking advantage of Impala/Hive metastores and YARN/Ozone storage.

### 3. ZERO-HARDCODE CONFIGURATION
**❌ FORBIDDEN: Hardcoded values anywhere in code**
- NO hardcoded paths or URIs (`s3a://...`, `abfs://...`, `hdfs://...`)
- NO specific database names or table locations in the main body.
- NO credentials

**✅ REQUIRED: All paths and configs from orchestrator context**
```python
def execute_task(spark, config):
    # Extract ALL values from config dictionary
    # Cloudera implementations often rely on the catalog configuration
    catalog = config.get('catalog_name', 'hive_metastore')
    schema = config.get('target_schema', 'default')
```

### 4. FUNCTION STRUCTURE (Mandatory)
**✅ ONLY expose `execute_task(spark, config)` - NO main() function**
```python
def execute_task(spark, config):
    """
    Direct transcription for {{asset_name}}.
    """
    import logging
    logger = logging.getLogger(__name__)
    
    try:
        # Business logic translated from source
        # ...
        
        return {
            "status": "SUCCESS"
        }
    except Exception as e:
        logger.error(f"Task failed: {str(e)}")
        raise
```

## 🎯 OUTPUT REQUIREMENTS
1. Return ONLY valid Cloudera compatible PySpark code.
2. Zero hardcoded credentials or paths.
3. Include L2L header.
4. Functional encapsulation via `execute_task(spark, config)`.
